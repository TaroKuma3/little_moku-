<h1>Mypage#show</h1>
<% if @user.public %>
  <h1><%= "成果物の製作者：#{@user.name}さん" %></h1>

  <h4><%= "#{@user.name}さんのMOKUアイテム" %></h4>
  <%= simple_format(@user.items) %>

  <h4><%= "#{@user.name}さんの自己紹介" %></h4>
  <%= simple_format(@user.introduce) %>

  <p>TwitterとかインスタとかFBとかのリンクが出せたらいいかも</p>
<% else %>
  <h3><%= "#{@user.name}さんは非公開ユーザーさんです" %></h3>
<% end %>
<h3><%= link_to("マイページへ戻る", "/mypage")%></h3>

